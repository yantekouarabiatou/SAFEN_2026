{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev && npm install && npm run production"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}